import UIKit

// List of Global References

/// reference to appDelegate.
let appDelegate = UIApplication.sharedApplication().delegate as! AppDelegate

/// Dictionary used in HGEncodables
typealias HGDICT = Dictionary<String, AnyObject>

/// Array used in HGEncodables
typealias HGARRAY = [HGDICT]

/// returns hgdict from AnyObject, used for HGEncodable
func hgdict(fromObject object: AnyObject, decoderName: String) -> HGDICT {
    if let dict = object as? HGDICT {
        return dict
    }
    HGReportHandler.shared.report("object: |\(object)| is not \(decoderName) mapable", type: .Error)
    return HGDICT()
}